import { LogisticaMarinaModel } from './logistica-marina.model.model';

describe('LogisticaMarinaModel', () => {
  it('should create an instance', () => {
    expect(new LogisticaMarinaModel()).toBeTruthy();
  });
});
